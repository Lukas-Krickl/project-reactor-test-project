application:
  include-error-causing-element.enabled: true
  car-repository:
    work-factor: 20ms
    mock-generator:
      amount-of-cars: 20
logging:
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"